# DetailCrop — 결정 기록

작성 2026-09-14. 무엇을 만들었는지가 아니라 **왜 그렇게 만들었는지**를 남긴다.
코드를 보면 "무엇"은 알 수 있지만 "왜"는 사라지기 때문이다.

> 이 문서에는 비밀값을 적지 않는다. 저장소가 공개이기 때문이다.
> `STATS_KEY`, `CF_API_TOKEN`, 관리자 URL은 Cloudflare 시크릿과
> 비밀번호 관리자에만 둔다.

---

## 1. 이게 무엇인가

이미지 여러 장에서 **한 장당 여러 구역**을 같은 비율로 잘라 zip 하나로
내보내는 도구. 전부 브라우저 안에서 처리하고 서버로 아무것도 보내지 않는다.

시작은 빅아트 작업이었다. 작품 34점에서 각각 6컷씩 확대컷을 뽑아야 했고,
손으로 하면 200번 넘게 같은 조작을 반복해야 했다.

**핵심 차별점은 "일괄 크롭"이 아니다.** 그건 이미 여러 도구가 한다.
남들이 안 하는 건 **한 이미지에서 서로 다른 여러 구역을 뽑는 것**이다.
포지셔닝을 바꿀 때 이 문장을 기준으로 삼을 것.

---

## 2. 조사로 확정한 사실

나중에 다시 조사하지 않도록 근거와 함께 남긴다.

### 브라우저 한계 (직접 측정, 데스크톱 크롬)

| 이미지 | 화소 | 파일 | 디코딩 | 6컷 내보내기 |
|---|---|---|---|---|
| 4000×3000 | 12MP | 6MB | 0.15초 | 정상 |
| 8000×6000 | 48MP | 24MB | 0.46초 | 5.9초 |
| 12000×9000 | 108MP | 54MB | 0.59초 | 정상 |
| 16000×12000 | 192MP | 96MB | 1.2초 | 정상 |

**캔버스 한 변 16384px가 하드 월.** 16384는 되고 20000은 실패한다.
iOS 사파리는 이보다 훨씬 낮고 조용히 실패하므로 임계값을 찍지 말 것.

### 인스타그램 피드 격자

**올리는 순서는 오른쪽 아래부터, 왼쪽 위가 마지막.** 프로필 격자가 최신 글을
왼쪽 위에 쌓기 때문이다. 출처 두 곳이 일치했다.
프로필 썸네일 비율은 출처마다 4:5와 1:1로 갈려서, 하나로 박지 않고
비율 컨트롤을 따르게 했다(기본 4:5).

### 경쟁 상황

- `bulkpictools.com` — 일괄 크롭 + 브라우저 처리 + zip. **우리 문구와 사실상 동일.**
  단, 한 이미지에서 여러 구역은 못 한다.
- `batchimagecrop.com` — 이미지마다 다른 크롭은 되지만 역시 한 장에 한 컷.
- 인스타 그리드 분할기는 전용 도메인만 10개 이상. 그쪽으로 정면 진입하면 안 된다.

### Cloudflare

- 공식 문서가 신규 프로젝트는 **Workers**를 쓰라고 명시. Pages 아님.
- 무료 플랜 **하루 10만 요청**, 초과 시 Error 1027로 사이트가 멈춘다.
  "트래픽 아무리 와도 공짜"는 사실이 아니다.
- Analytics Engine 무료: 하루 10만 기록 / 1만 조회.

---

## 3. 되돌리면 안 되는 결정

### 파일 크기가 아니라 픽셀로 제한한다

처음엔 40MB 상한이었는데 **틀린 걸 재고 있었다.** 3MB JPEG이 1억 화소일 수
있고 50MB PNG가 1200만 화소일 수 있다. 파일 크기는 메모리 비용과 무관하다.
지금은 200MB(사실상 형식적) + 한 변 16384px(진짜 한계)로 건다.

디코딩에 실패하면 "이미지가 아닙니다"가 아니라 **"이 브라우저가 열지 못했습니다"**
라고 말한다. iOS에서 실제로 일어나는 일이고, 원인을 정확히 알려주는 편이 낫다.

### 피드 격자 번호는 올리는 순서다

읽는 순서가 아니다. 9·8·7 / 6·5·4 / 3·2·1로 보이는 게 정상이다.
파일명도 `-post01`부터 붙는다. **버그 아님.** 위 2절 참조.

### 구글 애널리틱스를 붙이지 않는다

"아무것도 업로드하지 않습니다"가 이 도구의 판매 문구다. GA를 붙이면
자기모순이고 EU 방문자에겐 쿠키 배너까지 필요해진다.
자체 이벤트(`/e` → Analytics Engine)가 방문·국가·유입 경로를 이미 기록한다.

### 봇을 두 겹으로 거른다

페이지에서 `navigator.webdriver`가 켜져 있으면 비콘을 안 쏘고, Worker에서
봇 User-Agent를 버린다. 없을 때는 새 도메인을 훑는 스캐너가 방문자로
잡혔다(폴란드·프랑스·미국에서 들어오는 것처럼 보였다).
**2026-09-13 이전 숫자에는 스캐너가 섞여 있다.**

### 오늘 지표는 방문자에게 보이지 않는다

푸터의 오늘 방문 수는 관리자 키를 가진 브라우저에만 뜬다.
하루 방문이 한 자릿수일 때 공개 카운터는 사회적 증거가 **마이너스**다.
숫자가 커지면 공개로 돌리면 된다 — 한 줄이다.

### 저장소는 공개, 라이선스는 없음

정적 HTML이라 배포하는 순간 코드는 이미 공개된다. 비공개로 잠가도 지켜지는 게
없다. 라이선스를 안 붙이면 기본이 "저작권 전부 보유"라 재사용 권한은 없다.
**MIT 같은 걸 붙이면 안 된다.**

### 언어별 URL을 따로 둔다

자바스크립트 전환만으로는 검색엔진이 한 언어만 색인한다.
`/`, `/ko/`, `/ja/` 세 벌에 hreflang을 걸었다. 내용은 같고 기본 언어와
canonical만 다르다. **한 파일만 고치고 배포하면 안 된다** — 빌드로 세 벌을 만든다.

### 통계 키는 소모품이다

잃어버려도 새로 만들면 된다. 쌓인 데이터는 Analytics Engine에 따로 있어서
키와 무관하다. 진짜 지켜야 하는 건 Cloudflare 계정과 가비아 계정이다.

---

## 4. 내가(=AI가) 틀렸던 것

같은 실수를 반복하지 않도록 남긴다.

- **"일괄 크롭 도구가 없다"** — 있다. bulkpictools가 우리와 같은 문구로 이미 한다.
  차별점은 일괄이 아니라 "한 장에서 여러 구역"이다.
- **PL·US 방문을 사람으로 봤다** — 스캐너였고, 미국 접속 일부는 진단하느라
  페이지를 열어본 AI 자신이었다.
- **Web Analytics 도입을 과하게 반대했다** — 부작용 없다. 켜도 된다.
- **저장소 공개 여부를 길게 논쟁했다** — 매출에 영향 없는 사안이었다.
- **og 이미지 첫 구도 실패** — 그림을 가로로 잘라서 크롭 상자가 프레임 밖으로
  나갔다. "원본 + 6컷"을 나란히 놓는 구도로 다시 잡았다.

---

## 5. 아직 안 한 것

- **커스텀 도메인 연결** — `detailcrop.com`이 아직 DNS에서 안 잡힌다. 이게 0번.
- Google Search Console 등록 + 사이트맵 제출
- SEO 콘텐츠 글 3~5편 (도구 페이지 하나로는 검색어를 못 잡는다)
- 지인 배포 후 전환율 확인 (방문 대비 내보내기 비율이 진짜 신호)
- 6주 뒤 판단: 월 2천 미만이면 접어두기 / 1만 넘으면 시간 더 투자

---

## 6. 코드에서 주의할 곳

- **`template.html`이 원본이다.** `public/*/index.html` 세 개는 빌드 산출물이라
  직접 고치면 다음 빌드에 날아간다.
- 예시 이미지는 base64로 페이지에 박혀 있다(약 180KB). 히어로 배경과
  "예시로 해보기" 소스를 겸한다. **이 그림은 빅아트 작품이다** — 사이트를 팔거나
  넘길 일이 생기면 먼저 교체할 것.
- ZIP은 외부 라이브러리 없이 직접 만든다(무압축 store 방식). WebP·JPEG은 이미
  압축돼 있어 재압축 이득이 없고, 이러면 CDN 장애와 무관해진다.
- 다운스케일은 절반씩 단계적으로 한다. 8000px에서 1415px로 한 번에 줄이면 뭉개진다.
- `/stats`와 `/api/today`는 키가 틀리면 **SQL을 돌리기 전에** 404를 낸다.
  틀린 시도가 조회 한도를 먹지 않게 하려는 것이다.

---

## 7. 수평 조절 (2026-09-14)

지인 아이디어. 갤럭시 갤러리의 수평 맞추기와 같은 개념으로, **크롭이 아니라
사진 자체**를 ±15° 돌린다. 크롭 회전은 일부러 뺐다 — 기울어진 상자로 잘라내면
결과물은 결국 같은 그림인데 조작만 어려워진다.

회전하면 네 모서리에 빈 삼각형이 생긴다. 이걸 결과물에 안 들어오게 하려고
**회전된 사진 안에 완전히 들어가는 최대 직사각형**을 작업 영역으로 잡는다.
이 사각형은 원본과 비율이 같아서 기존 비율 계산·피드 분할이 전부 그대로
동작하고, 각도가 커질수록 픽셀 수만 줄어든다(3:2에서 6°면 약 13%, 15°면 약 26%).

정확히 딱 맞추면 바깥쪽 모서리 픽셀 두 개가 절반만 덮여서 PNG로 뽑을 때
반투명 점으로 남는다. 그래서 **1px 안쪽으로 당겨서** 쓴다.

검증: rot=0 결과물은 PIL 크롭과 픽셀 차이 0 (기존 동작 무변화). 6°·15°
전체 프레임 PNG에서 반투명 픽셀 0개, 크기는 계산값과 정확히 일치. 기울인
사진의 수평선이 결과물에서 0.0°로 나온다.

---

## 8. 원근 보정 (2026-09-14)

7번의 회전에 이어 갤럭시 기울기 패널의 나머지 두 개 — **수평·수직 원근(키스톤)**
슬라이더를 넣었다. 올려다보며 찍어 위가 좁아진 벽·액자·건물을 정면처럼 편다.

세 슬라이더는 이미지당 **3×3 호모그래피 하나**로 합쳐진다. 이 행렬 하나가
작업 화면(CSS `matrix3d`)·컷 미리보기·내보내기를 전부 굴린다. 세 군데가
서로 어긋날 수가 없는 구조다.

- **작업 영역**: 변형하면 모서리에 빈 공간이 생기므로, 변형된 사각형 안에
  완전히 들어가는 최대 직사각형(원본 비율 유지)을 이진탐색으로 찾는다.
  판정은 네 변의 반평면 클리핑. 비율이 유지되니 기존 비율 계산과 피드 분할은
  손댈 필요가 없었다.
- **세 값이 모두 0이면** 별도 경로로 빠져서 예전과 바이트 단위로 동일하게 나온다.
- **회전만이면** 아핀이라 캔버스가 처리한다. 키스톤이 섞이면 캔버스로는
  불가능해서 **WebGL**로 그린다. 사각형 하나에 픽셀마다 역호모그래피.
- **분할**: 출력을 블록으로 쪼개서 각 블록이 읽는 원본 조각이 텍스처 한도
  안에 들어오게 한다. 한도는 헤드리스 크롬에서 8192, 휴대폰은 4096인 경우도
  많은데 **넘겨도 드라이버가 조용히 버린다**(에러가 안 뜬다). 처음엔 가로 전체
  띠로 잘랐다가 9000px 이미지에서 이 한도를 넘겼고, 그래서 2차원 분할로 바꿨다.
- **WebGL이 없으면** 원근 두 줄을 숨긴다. 회전은 그대로 된다.

검증: 5400만 화소 원본을 전체 프레임으로 변형해 CPU 레퍼런스(numpy 이중선형)와
비교했을 때 블록 경계 포함 전 구간 오차 0.6/255 이하. 기준점 19개의 위치 오차는
평균 0.08px, 최대 0.17px. 화면 쪽도 예측 좌표 19곳 전부 맞는 색이 찍혔다.

**컷 미리보기**도 같은 행렬로 다시 만들었다. 이전에는 원본을 배경 이미지로
잘라 보여줘서 기울기가 반영되지 않았다. 지금은 미리보기 안에 원본 `<img>`를
넣고 스테이지와 같은 변환을 축소 배율로 걸어서, 슬라이더를 움직이는 즉시
따라온다. 프레임은 `border`가 아니라 이미지 위에 덮는 방식으로 그린다.
`border`를 쓰면 미리보기 상자가 실제 컷 비율에서 4px 어긋난다.

---

## 9. 정렬 보정을 접어둔 이유 (2026-09-14)

회전·원근 세 개는 **기본으로 닫혀 있는 섹션**(`#alignPanel`)에 넣었다. 이 툴의
본래 동작은 "상자 끌어서 확대컷 잡기"이고, 사진을 돌리고 원근을 펴는 건 결이
다른 작업이다. 대부분의 방문은 필요 없는데 슬라이더 세 줄이 늘 보이면 툴이
어려워 보인다.

닫혀 있는 것과 숨기는 것은 다르다. 현재 이미지에 값이 들어가 있으면 **접힌
헤더에 그 숫자가 앰버로 뜬다**(`-6.0° · 수직 +48`). 안 그러면 화면 속 사진이
왜 기울어져 있는지 알 방법이 없다. 이미지를 바꾸면 헤더도 그 이미지 값으로
따라 바뀐다.

**나중에 유료 기능으로 돌릴 자리다.** 필요한 게 전부 이 섹션 안에 들어 있어서,
잠그는 건 이 패널 하나를 안 열어주는 것으로 끝난다. 결제 자체는 아직 미착수.

---

## 10. 영상 페이지를 이미지 페이지와 같은 모양으로 (2026-09-15)

영상 페이지는 원래 "영상 하나, 컷 최대 4개"였다. 이미지 페이지와 같은 작업
방식으로 맞췄다.

**예시 영상으로 시작한다.** 이미지 페이지가 예시 이미지를 물고 뜨는 것과
같다. 빈 드롭존만 있는 화면은 이 도구가 무엇을 하는지 한 줄도 보여주지
못한다. 부팅하면 예시 영상이 컷 3개와 함께 편집기에 올라와 있고, 설명 칸은
옆에 남는다. 예시를 열지 못하는 브라우저(H.264 디코더가 없는 크로미움 빌드,
플레이라이트가 들고 다니는 게 그렇다)는 조용히 빈 화면으로 떨어진다 —
방문자를 오류 메시지로 맞을 일이 아니다.

**컷은 6개까지.** 이미지 페이지와 같은 한계, 같은 상자 색 여섯 개
(`--b1`…`--b6`). 숫자 키도 1–6으로 같다.

**영상도 여러 개를 한 번에.** 필름스트립을 스테이지 위에 두고 넘기며
작업한다. 확정한 영상에는 초록 점이 붙는다 — 이미지 페이지 사용법 5번
그대로다. 전부 잡았으면 zip 하나로 내려받는다. 이미지 페이지의 손으로 쓴
zip 작성기(store 방식)를 그대로 가져왔다. mp4는 이미 압축되어 있어서 압축할
이유가 없고, 이러면 CDN이 죽어도 내보내기가 멈추지 않는다.

**비율은 페이지가 아니라 컷이 가진다.** 이전에는 설정의 크롭 비율이 모든
컷에 일괄 적용되고, 컷별 비율은 접힌 섹션에 따로 있었다. 영상에서는 한
소스에서 쇼츠·피드·캐러셀을 동시에 뽑는 게 기본 동작이라, 일괄이 기본값인
게 거꾸로였다. 이제 비율 버튼은 **선택한 컷**만 바꾸고, 일괄로 맞추고 싶으면
'모든 컷에 같은 비율' 버튼을 누른다. 출력 크기 '고정'도 컷마다 자기 비율에
맞는 규격을 가진다.

**구간은 컷마다 한 줄.** 손잡이 두 개를 한 트랙 위에 겹쳐두고 가는 줄로
구간을 표시하던 방식은, 컷이 셋만 되어도 서로 붙어 무엇을 잡는지 알 수
없었다. 컷마다 높이 26px짜리 자기 줄을 준다. 가운데를 끌면 길이를 유지한 채
구간이 통째로 움직이고, 양 끝을 끌면 구간이 바뀌고, 눈금자를 끌면 재생
위치가 움직인다. **줄을 클릭하면 그 컷이 선택된다** — 화면의 상자를 찾아
누르지 않아도 되도록, 선택이 양쪽에서 된다.

**전환은 로고 아래에.** 두 도구를 오가는 버튼이 오른쪽 헤더 버튼들 사이에
섞여 있었다. 로고 바로 아래 탭 두 개(`이미지` / `영상`)로 옮기고 현재
페이지에 `aria-current="page"`를 준다. 로고 자체도 자기 페이지 홈으로 가는
링크가 됐다. 두 페이지에 같은 마크업이 들어가므로, 한쪽만 고치지 말 것.

**`build.py`가 영상 페이지도 만든다.** 그전까지 `public/video/**`는 세 벌을
손으로 맞춰야 했고, `build.py`의 출력 경로(`detailcrop/public`)는 저장소
구조와 맞지도 않았다. 이제 `template-video.html` 하나를 고치고
`python3 build.py`를 돌린다. 출력은 `public/`.
